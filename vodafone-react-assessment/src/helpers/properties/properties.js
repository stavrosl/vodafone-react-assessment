const properties = [
    {
        slideProps: {
            duration: 5000,
            transitionDuration: 500,
            infinite: true,
            indicators: true,
            arrows: false,
            scale: 1.4
        }
    }
]

export default properties;